#include<stdio.h>
int main(){
	  
	  float c, k, f;
	  
	printf("Digite a temperatura em celsius: ");
	scanf("%f", &c);
	
	
	
	printf("Conversao de celsius para kelvin: %.2f\n", k + (c + 273) );
	printf("Conversao de celsius para fahrenheit %.2f", f + ((c * 1.8) + 32)  );
}
